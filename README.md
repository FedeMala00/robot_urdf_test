This ROS2 package is used to simulate a mobile robot in a 3D environnment using Gazebo and Rviz (required packages)  

In order to start the simulation use the command:  
```bash 
ros2 launch robot_urdf_test gazebo2.launch.py
```
